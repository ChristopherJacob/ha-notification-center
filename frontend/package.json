{
  "name": "notification-center-frontend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "rollup -c && cp -r dist/* ../custom_components/notification_center/frontend/",
    "dev": "rollup -c --watch",
    "clean": "rm -rf dist ../custom_components/notification_center/frontend/*"
  },
  "dependencies": {
    "lit": "^3.1.0"
  },
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^15.2.3",
    "@rollup/plugin-typescript": "^11.1.6",
    "rollup": "^4.9.0",
    "typescript": "^5.3.0",
    "tslib": "^2.6.0"
  }
}
